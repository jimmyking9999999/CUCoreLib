{
  "id": "statuses",
  "label": "Statuses",
  "crumb": "Player",
  "title": "Statuses",
  "lead": "Reference for status effects, common status state, and the gameplay hooks mods usually read or modify.",
  "appUrl": "/docs/statuses/",
  "machineUrl": "/api/topics/statuses.json",
  "bodyHtml": "\n    <section class=\"lesson-card\">\n      <h2>What statuses are for</h2>\n      <p>Use CUCoreLib statuses when your mod wants new per-instance fields on a vanilla <span class=\"inline-code\">Body</span> or <span class=\"inline-code\">Limb</span> without editing the game's classes. The storage is backed by <span class=\"inline-code\">ConditionalWeakTable</span>, but the supported consumer surface is the one-line extension call <span class=\"inline-code\">body.GetStatus&lt;TStatus&gt;()</span> or <span class=\"inline-code\">limb.GetStatus&lt;TStatus&gt;()</span>.</p>\n      <pre><code>Body body = PlayerCamera.main.body;\nSunstrokeStatus status = body.GetStatus&lt;SunstrokeStatus&gt;();\n\nstatus.ExposureSeconds += Time.deltaTime;</code></pre>\n    </section>\n\n    <section class=\"lesson-card\">\n      <h2>Define a body status with new fields</h2>\n      <p>Body-first is the simplest pattern. Inherit from <span class=\"inline-code\">BodyStatus</span>, add the fields your mod needs, and optionally add <span class=\"inline-code\">[StatusOptions]</span> when you want a stable save key or want to disable saving for transient data.</p>\n      <pre><code>[StatusOptions(Key = \"com.yourname.sunstroke\", SaveEnabled = false)]\n      // The above ^ is optional, it defaults to being saved naturally without need for a key \n      public sealed class SunstrokeStatus : BodyStatus\n      {\n          public float ExposureSeconds;\n          public float CoolingGraceSeconds;\n          public bool WarnedPlayer;\n      }</code></pre>\n      <p>These fields are ordinary C# fields. Think of them as extra body data for your mod.</p>\n    </section>\n\n    <section class=\"lesson-card\">\n      <h2>CCLBody is for vanilla-owned calculations</h2>\n      <p>Use a normal <span class=\"inline-code\">BodyStatus</span> when your mod needs attached custom state. Use <span class=\"inline-code\">CCLBody</span> when the game recalculates a public vanilla <span class=\"inline-code\">Body</span> field and you want CUCoreLib to inject your value into that formula seam for you.</p>\n      <pre><code>CCLBody.TotalEncumberance += 10f;\nCCLBody.MaxEncumberance += 1.5f;\nCCLBody.BloodPressure -= 12f;</code></pre>\n      <p>The current built-in surface is intentionally explicit. Only fields CUCoreLib actually patches are supported here.</p>\n    </section>\n\n    <section class=\"lesson-card\">\n      <h2>Read vanilla fields and update your own state in Update()</h2>\n      <p>A common pattern is: read real vanilla body values, update your attached status, then write the gameplay effect back into other vanilla body fields. This keeps your custom logic self-contained while still using the real game simulation.</p>\n      <pre><code>[HarmonyPatch(typeof(Body), \"Update\")]\npublic static class BodyUpdateStatusPatch\n{\n    [HarmonyPostfix]\n    private static void Postfix(Body __instance)\n    {\n       \n        SunstrokeStatus status = __instance.GetStatus&lt;SunstrokeStatus&gt;();\n\n        if (__instance.temperature &gt; 39.2f)\n        {\n            status.ExposureSeconds += Time.deltaTime;\n        }\n            \n        if (status.ExposureSeconds &gt; 30f)\n        {\n            __instance.thirst = Mathf.Max(0f, __instance.thirst - Time.deltaTime * 1.5f);\n            __instance.stamina = Mathf.Max(0f, __instance.stamina - Time.deltaTime * 2f);\n        }\n    }\n}</code></pre>\n    </section>\n\n    <section class=\"lesson-card\">\n      <h2>Limb statuses use the same pattern</h2>\n      <p>Per-limb state works the same way, just from <span class=\"inline-code\">LimbStatus</span> and <span class=\"inline-code\">limb.GetStatus&lt;T&gt;()</span>. Use this when your extra data truly belongs to one wound, one treatment timer, one custom fracture rule, or one custom infection system on a specific limb.</p>\n      <pre><code>public sealed class FrostbiteStatus : LimbStatus\n      {\n          public float ExposureSeconds;\n          public bool HasNerveDamage;\n      }\n\n      Limb foreleg = body.limbs[2];\n      FrostbiteStatus frostbite = foreleg.GetStatus&lt;FrostbiteStatus&gt;();</code></pre>\n    </section>\n\n    <section class=\"lesson-card\">\n      <h2>When to use statuses instead of other storage</h2>\n      <div class=\"table-wrap\">\n        <table class=\"field-table\">\n          <thead>\n            <tr><th>Use case</th><th>Best place</th></tr>\n          </thead>\n          <tbody>\n            <tr><td>Per-body runtime fields that act like new vanilla body variables</td><td><span class=\"inline-code\">BodyStatus</span></td></tr>\n            <tr><td>Public vanilla body fields that vanilla recalculates every frame or periodic pass</td><td><span class=\"inline-code\">CCLBody</span></td></tr>\n            <tr><td>Per-limb runtime fields tied to one limb instance</td><td><span class=\"inline-code\">LimbStatus</span></td></tr>\n            <tr><td>Registration-time metadata for a custom item/tile/building definition</td><td><span class=\"inline-code\">CustomData</span> on the registered definition</td></tr>\n            <tr><td>Large mod-wide systems not owned by one body/limb instance</td><td>Your own manager plus <span class=\"inline-code\">SaveRegistry</span> provider if needed</td></tr>\n          </tbody>\n        </table>\n      </div>\n      <p>Statuses are meant for instance-attached mutable state, not generic global storage.</p>\n      <p>Given that there will only ever be one <span class=\"inline-code\">Body</span>, you technically can? But it's not great to do so regardless. :p</p>\n    </section>\n\n    <section class=\"lesson-card\">\n      <h2>Moodles</h2>\n      <p>A status can optionally provide player-facing UI through <span class=\"inline-code\">MoodleRegistry</span>. If you want the shortest pattern, call <span class=\"inline-code\">MoodleRegistry.AddMoodle(...)</span> directly from your update logic whenever the condition is active.</p>\n      <pre><code>if (body.respiratoryRate &lt; 50f)\n{\n    MoodleRegistry.AddMoodle(\n        1,\n        \"hypoventilation\",\n        \"Hypoventilation\",\n        \"You're breathing unusually slowly.\",\n        important: true\n    );\n}</code></pre>\n      <p>Use this when the status should show a normal moodle-like warning. For broader moodle behavior and icon expectations, see the <span class=\"inline-code\">Moodles</span> page.</p>\n    </section>\n  ",
  "bodyText": "What statuses are for Use CUCoreLib statuses when your mod wants new per-instance fields on a vanilla Body or Limb without editing the game's classes. The storage is backed by ConditionalWeakTable , but the supported consumer surface is the one-line extension call body.GetStatus<TStatus>() or limb.GetStatus<TStatus>() . Body body = PlayerCamera.main.body; SunstrokeStatus status = body.GetStatus<SunstrokeStatus>(); status.ExposureSeconds += Time.deltaTime; Define a body status with new fields Body-first is the simplest pattern. Inherit from BodyStatus , add the fields your mod needs, and optionally add [StatusOptions] when you want a stable save key or want to disable saving for transient data. [StatusOptions(Key = \"com.yourname.sunstroke\", SaveEnabled = false)] // The above ^ is optional, it defaults to being saved naturally without need for a key public sealed class SunstrokeStatus : BodyStatus { public float ExposureSeconds; public float CoolingGraceSeconds; public bool WarnedPlayer; } These fields are ordinary C# fields. Think of them as extra body data for your mod. CCLBody is for vanilla-owned calculations Use a normal BodyStatus when your mod needs attached custom state. Use CCLBody when the game recalculates a public vanilla Body field and you want CUCoreLib to inject your value into that formula seam for you. CCLBody.TotalEncumberance += 10f; CCLBody.MaxEncumberance += 1.5f; CCLBody.BloodPressure -= 12f; The current built-in surface is intentionally explicit. Only fields CUCoreLib actually patches are supported here. Read vanilla fields and update your own state in Update() A common pattern is: read real vanilla body values, update your attached status, then write the gameplay effect back into other vanilla body fields. This keeps your custom logic self-contained while still using the real game simulation. [HarmonyPatch(typeof(Body), \"Update\")] public static class BodyUpdateStatusPatch { [HarmonyPostfix] private static void Postfix(Body __instance) { SunstrokeStatus status = __instance.GetStatus<SunstrokeStatus>(); if (__instance.temperature > 39.2f) { status.ExposureSeconds += Time.deltaTime; } if (status.ExposureSeconds > 30f) { __instance.thirst = Mathf.Max(0f, __instance.thirst - Time.deltaTime * 1.5f); __instance.stamina = Mathf.Max(0f, __instance.stamina - Time.deltaTime * 2f); } } } Limb statuses use the same pattern Per-limb state works the same way, just from LimbStatus and limb.GetStatus<T>() . Use this when your extra data truly belongs to one wound, one treatment timer, one custom fracture rule, or one custom infection system on a specific limb. public sealed class FrostbiteStatus : LimbStatus { public float ExposureSeconds; public bool HasNerveDamage; } Limb foreleg = body.limbs[2]; FrostbiteStatus frostbite = foreleg.GetStatus<FrostbiteStatus>(); When to use statuses instead of other storage Use case Best place Per-body runtime fields that act like new vanilla body variables BodyStatus Public vanilla body fields that vanilla recalculates every frame or periodic pass CCLBody Per-limb runtime fields tied to one limb instance LimbStatus Registration-time metadata for a custom item/tile/building definition CustomData on the registered definition Large mod-wide systems not owned by one body/limb instance Your own manager plus SaveRegistry provider if needed Statuses are meant for instance-attached mutable state, not generic global storage. Given that there will only ever be one Body , you technically can? But it's not great to do so regardless. :p Moodles A status can optionally provide player-facing UI through MoodleRegistry . If you want the shortest pattern, call MoodleRegistry.AddMoodle(...) directly from your update logic whenever the condition is active. if (body.respiratoryRate < 50f) { MoodleRegistry.AddMoodle( 1, \"hypoventilation\", \"Hypoventilation\", \"You're breathing unusually slowly.\", important: true ); } Use this when the status should show a normal moodle-like warning. For broader moodle behavior and icon expectations, see the Moodles page.",
  "codeTitle": "StatusesExample.cs",
  "codeLanguage": "csharp",
  "code": "using CUCoreLib.Data;\nusing CUCoreLib.Helpers;\nusing CUCoreLib.Registries;\nusing HarmonyLib;\nusing UnityEngine;\n\n[StatusOptions(Key = \"com.yourname.sunstroke\", SaveEnabled = true)]\npublic sealed class SunstrokeStatus : BodyStatus\n{\n    public float ExposureSeconds;\n    public float CoolingGraceSeconds;\n    public bool WarnedPlayer;\n}\n\nprivate static void ConfigureBodyFormula(Body body)\n{\n    CCLBody.TotalEncumberance += 5f;\n    CCLBody.MaxEncumberance += 1.5f;\n    CCLBody.BloodPressure -= 10f;\n}\n\n[StatusOptions(Key = \"com.yourname.sunstroke\", SaveEnabled = true)]\npublic sealed class SunstrokeStatus : BodyStatus\n{\n    public float ExposureSeconds;\n    public float CoolingGraceSeconds;\n    public bool WarnedPlayer;\n}\n\n[HarmonyPatch(typeof(Body), \"Update\")]\npublic static class BodyUpdateStatusPatch\n{\n    private static void Postfix(Body __instance)\n    {\n\n        SunstrokeStatus status = __instance.GetStatus<SunstrokeStatus>();\n\n        if (__instance.temperature > 39.2f)\n        {\n            status.ExposureSeconds += Time.deltaTime;\n            status.CoolingGraceSeconds = 6f;\n        }\n        else if (status.CoolingGraceSeconds > 0f)\n        {\n            status.CoolingGraceSeconds -= Time.deltaTime;\n        }\n        else\n        {\n            status.ExposureSeconds = Mathf.Max(0f, status.ExposureSeconds - Time.deltaTime * 4f);\n        }\n\n        if (status.ExposureSeconds > 30f)\n        {\n            __instance.thirst = Mathf.Max(0f, __instance.thirst - Time.deltaTime * 1.5f);\n            __instance.stamina = Mathf.Max(0f, __instance.stamina - Time.deltaTime * 2f);\n        }\n\n        if (status.ExposureSeconds > 50f)\n        {\n            __instance.sicknessAmount = Mathf.Clamp(__instance.sicknessAmount + Time.deltaTime * 3f, 0f, 100f);\n        }\n\n        if (!status.WarnedPlayer && status.ExposureSeconds > 45f)\n        {\n            status.WarnedPlayer = true;\n            __instance.talker.Say(\"I need shade.\");\n        }\n    }\n}\n\n[StatusOptions(Key = \"com.yourname.temporaryHeatPulse\", SaveEnabled = false)]\npublic sealed class TemporaryHeatPulseStatus : BodyStatus\n{\n    public float RemainingSeconds;\n}\n\n[StatusOptions(Key = \"com.yourname.leadpoisoning\", SaveEnabled = true)]\npublic sealed class RespiratoryWarningStatus : BodyStatus\n{\n    public float SlowBreathingSeconds;\n}\n\n[HarmonyPatch(typeof(Body), \"Update\")]\npublic static class RespiratoryWarningPatch\n{\n    private static void Postfix(Body __instance)\n    {\n\n        RespiratoryWarningStatus status = __instance.GetStatus<RespiratoryWarningStatus>();\n\n        if (__instance.respiratoryRate < 50f)\n        {\n            status.SlowBreathingSeconds += Time.deltaTime;\n        }\n        else\n        {\n            status.SlowBreathingSeconds = 0f;\n        }\n    }\n}",
  "interactiveDefaults": null
}
