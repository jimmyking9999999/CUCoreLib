{
  "id": "liquid-tiles",
  "label": "Liquid Tiles",
  "crumb": "World",
  "title": "Liquid Tile API",
  "lead": "Register swimmable world liquids with custom visuals, touch effects, drinking behavior, and worldgen.",
  "appUrl": "/docs/liquid-tiles/",
  "machineUrl": "/api/topics/liquid-tiles.json",
  "bodyHtml": "\n    <section class=\"lesson-card\">\n      <h2>Liquids? Tiles? Why not combine the two?</h2>\n      <p><span class=\"inline-code\">LiquidTileRegistry</span> is CUCoreLib's world-liquid API. It registers custom fluid cells in the vanilla <span class=\"inline-code\">FluidManager</span>, gives them a stable custom world byte, and patches the normal liquid systems so they can render, be drunk, fill containers, and affect bodies standing in them.</p>\n      <p>This is different from <a href=\"/docs/liquids/\" data-page=\"liquids\">Liquid API</a>. A liquid registration defines what a logical liquid is. A liquid tile registration defines how that liquid behaves when it exists as a pool in the world.</p>\n      <pre><code>LiquidRegistry.Register(\"acidbrine\", new CustomLiquidInfo\n{\n    name = \"Acid brine\",\n    description = \"A shimmering pool of caustic industrial runoff.\",\n    color = new Color(0.72f, 0.96f, 0.28f),\n    valuePerLiter = 8f,\n    onDrink = (ml, body) =>\n    {\n        float dose = ml * 0.01f;\n        body.Drink(dose * 2f);\n        body.sicknessAmount += dose * 9f;\n        body.happiness -= dose * 3f;\n    }\n});\n\nLiquidTileRegistry.Register(\"acidbrinepool\", new CustomLiquidTileInfo\n{\n    LiquidId = \"acidbrine\",\n    FillLiquidId = \"acidbrine\",\n    Tint = new Color(0.8f, 1f, 0.45f, 1f),\n    TemperaturePerSecond = 4f,\n    SicknessPerSecond = 6f,\n    WetnessPerSecond = 30f,\n    SlipPerSecond = 0.35f,\n    SpawnAmount = 0.15f\n});</code></pre>\n    </section>\n\n    <section class=\"lesson-card\">\n      <h2>What gets patched for you</h2>\n      <p>Once registered, CUCoreLib hooks the tile into the normal game liquid flow. <span class=\"inline-code\">FluidManager.WaterInfo</span> reads your buoyancy and drag, <span class=\"inline-code\">LiquidColor</span> and <span class=\"inline-code\">LiquidName</span> use your registered display data, <span class=\"inline-code\">DrinkLiquid</span> can consume the pool through the linked liquid, and <span class=\"inline-code\">Body.HandleVariableUpdates</span> applies touch effects every frame.</p>\n      <p>That means you do not need a separate Harmony patch just to make a custom swamp, acid pond, algae bath, or healing puddle act like a real world liquid.</p>\n    </section>\n\n    <section class=\"lesson-card\">\n      <h2>CustomLiquidTileInfo</h2>\n      <p><span class=\"inline-code\">CustomLiquidTileInfo</span> controls the world-facing behavior of the liquid tile. Most mods only need to set the linked liquid IDs, visuals, touch effects, and maybe worldgen.</p>\n      <div class=\"table-wrap\">\n        <table class=\"field-table\">\n          <thead><tr><th>Field</th><th>Type</th><th>What it does</th></tr></thead>\n          <tbody>\n            <tr><td><span class=\"inline-code\">LiquidId</span></td><td><span class=\"inline-code\">string</span></td><td>Logical liquid used for display color fallback, drinking, and tooltip locale lookup. Defaults to the tile registration ID.</td></tr>\n            <tr><td><span class=\"inline-code\">FillLiquidId</span></td><td><span class=\"inline-code\">string</span></td><td>Liquid ID loose containers receive when they fill from this tile. Defaults to <span class=\"inline-code\">LiquidId</span>.</td></tr>\n            <tr><td><span class=\"inline-code\">Buoyancy</span></td><td><span class=\"inline-code\">float</span></td><td>Returned through <span class=\"inline-code\">FluidManager.WaterInfo</span> so bodies float or sink differently in this liquid.</td></tr>\n            <tr><td><span class=\"inline-code\">Drag</span></td><td><span class=\"inline-code\">float</span></td><td>Movement resistance used while inside the liquid.</td></tr>\n            <tr><td><span class=\"inline-code\">PushBodies</span></td><td><span class=\"inline-code\">bool</span></td><td>If true, CUCoreLib marks the body as <span class=\"inline-code\">inWater</span> while touching the tile.</td></tr>\n            <tr><td><span class=\"inline-code\">WetnessPerSecond</span></td><td><span class=\"inline-code\">float</span></td><td>Wetness added to the body each second.</td></tr>\n            <tr><td><span class=\"inline-code\">TemperaturePerSecond</span></td><td><span class=\"inline-code\">float</span></td><td>Temperature change applied every second while touching the tile.</td></tr>\n            <tr><td><span class=\"inline-code\">SicknessPerSecond</span></td><td><span class=\"inline-code\">float</span></td><td>Sickness buildup per second.</td></tr>\n            <tr><td><span class=\"inline-code\">DirtynessPerSecond</span></td><td><span class=\"inline-code\">float</span></td><td>Dirtyness added per second.</td></tr>\n            <tr><td><span class=\"inline-code\">DisinfectPerSecond</span></td><td><span class=\"inline-code\">float</span></td><td>Refreshes limb disinfect time while standing in the liquid.</td></tr>\n            <tr><td><span class=\"inline-code\">SlipPerSecond</span></td><td><span class=\"inline-code\">float</span></td><td>Adds to <span class=\"inline-code\">body.liquidSlipTime</span> for slippery liquids.</td></tr>\n            <tr><td><span class=\"inline-code\">RagdollBarDrainPerSecond</span></td><td><span class=\"inline-code\">float</span></td><td>Subtracts from <span class=\"inline-code\">body.liquidRagdollBar</span> over time.</td></tr>\n            <tr><td><span class=\"inline-code\">ConsumeOnDrink</span></td><td><span class=\"inline-code\">bool</span></td><td>If true, drinking removes the world liquid cell after use.</td></tr>\n            <tr><td><span class=\"inline-code\">ConsumeOnFill</span></td><td><span class=\"inline-code\">bool</span></td><td>Whether filling containers should consume the world liquid tile through the game's normal fill flow.</td></tr>\n            <tr><td><span class=\"inline-code\">OnDrinkOverride</span></td><td><span class=\"inline-code\">LiquidType.OnDrink</span></td><td>Optional direct drink callback for the tile. If unset, the linked <span class=\"inline-code\">LiquidId</span> <span class=\"inline-code\">onDrink</span> is used instead.</td></tr>\n            <tr><td><span class=\"inline-code\">OnEnter</span></td><td><span class=\"inline-code\">Action&lt;Body, LiquidTileTouchContext&gt;</span></td><td>Called once when a body steps into this liquid tile.</td></tr>\n            <tr><td><span class=\"inline-code\">OnTouch</span></td><td><span class=\"inline-code\">Action&lt;Body, LiquidTileTouchContext&gt;</span></td><td>Called every frame while a body remains on the tile, after the built-in wetness/sickness/slip effects.</td></tr>\n            <tr><td><span class=\"inline-code\">OnExit</span></td><td><span class=\"inline-code\">Action&lt;Body, LiquidTileTouchContext&gt;</span></td><td>Called once when a body leaves the tile.</td></tr>\n          </tbody>\n        </table>\n      </div>\n    </section>\n\n    <section class=\"lesson-card\">\n      <h2>Visuals</h2>\n      <p>Custom liquid tiles keep using the game's fluid rendering path, but you can steer how the visuals are sourced. The final display color is the linked liquid's color multiplied by <span class=\"inline-code\">Tint</span>.</p>\n      <p>When you need a real custom surface instead of \"vanilla water, but with a different texture\", use <span class=\"inline-code\">LiquidVisualHelper</span> to build a dedicated <span class=\"inline-code\">VisualMaterial</span> from an embedded texture, loose file, or explicit shader name.</p>\n      <pre><code>Material acidMaterial = LiquidVisualHelper.CreateLiquidMaterialFromEmbeddedTexture(\n    \"Images.acidbrine-surface.png\",\n    shaderName: \"Legacy Shaders/Particles/Alpha Blended\"\n);\n\nLiquidTileRegistry.Register(\"acidbrinepool\", new CustomLiquidTileInfo\n{\n    LiquidId = \"acidbrine\",\n    FillLiquidId = \"acidbrine\",\n    VisualMode = LiquidTileVisualMode.Material,\n    VisualMaterial = acidMaterial,\n    Tint = Color.white\n});</code></pre>\n      <div class=\"table-wrap\">\n        <table class=\"field-table\">\n          <thead><tr><th>Field</th><th>Type</th><th>What it does</th></tr></thead>\n          <tbody>\n            <tr><td><span class=\"inline-code\">VisualMode</span></td><td><span class=\"inline-code\">LiquidTileVisualMode</span></td><td>Chooses whether the tile reuses an existing vanilla fluid visual, a custom material, a sprite texture, or a generated high-res image.</td></tr>\n            <tr><td><span class=\"inline-code\">ExistingVisualLiquidByte</span></td><td><span class=\"inline-code\">byte</span></td><td>Base vanilla liquid visual to clone from. Defaults to <span class=\"inline-code\">1</span> which is water.</td></tr>\n            <tr><td><span class=\"inline-code\">Tint</span></td><td><span class=\"inline-code\">Color</span></td><td>Multiplier applied to the linked liquid color for fluid rendering and tile tooltip color.</td></tr>\n            <tr><td><span class=\"inline-code\">VisualMaterial</span></td><td><span class=\"inline-code\">Material</span></td><td>Material override used when <span class=\"inline-code\">VisualMode</span> is <span class=\"inline-code\">Material</span>. This is the full custom surface path.</td></tr>\n            <tr><td><span class=\"inline-code\">VisualSprite</span></td><td><span class=\"inline-code\">Sprite</span></td><td>Sprite texture override used when <span class=\"inline-code\">VisualMode</span> is <span class=\"inline-code\">Sprite</span>. It still reuses the current fluid material.</td></tr>\n            <tr><td><span class=\"inline-code\">HighResImage</span></td><td><span class=\"inline-code\">Texture2D</span></td><td>Texture override used when <span class=\"inline-code\">VisualMode</span> is <span class=\"inline-code\">HighResImageGenerated</span>. It still reuses the current fluid material.</td></tr>\n          </tbody>\n        </table>\n      </div>\n      <p>Most mods should start with <span class=\"inline-code\">ExistingLiquidPlusTint</span> and move to <span class=\"inline-code\">VisualMaterial</span> when the liquid needs a truly distinct surface look or a custom shader path.</p>\n    </section>\n\n    <section class=\"lesson-card\">\n      <h2>Placement and worldgen</h2>\n      <p>Use <span class=\"inline-code\">LiquidTileRegistry.Place(id, blockPos)</span> to place one custom liquid cell into an empty world block, or <span class=\"inline-code\">FloodFill(id, start, maxFill)</span> to spread the liquid outward using the vanilla fill simulation.</p>\n      <p>For terrain generation, set <span class=\"inline-code\">SpawnAmount</span> above <span class=\"inline-code\">0f</span>. CUCoreLib patches <span class=\"inline-code\">WorldGeneration.GenerateOres()</span> and calls <span class=\"inline-code\">world.PlaceLiquids(...)</span> for each registered tile that matches the current biome layer mask.</p>\n      <div class=\"table-wrap\">\n        <table class=\"field-table\">\n          <thead><tr><th>Field</th><th>Type</th><th>What it does</th></tr></thead>\n          <tbody>\n            <tr><td><span class=\"inline-code\">SpawnAmount</span></td><td><span class=\"inline-code\">float</span></td><td>How often the liquid generates during world creation. Values above <span class=\"inline-code\">0f</span> opt into generation.</td></tr>\n            <tr><td><span class=\"inline-code\">SpawnLayers</span></td><td><span class=\"inline-code\">int</span></td><td>Layer mask controlling which biome depths can spawn this liquid. Use <span class=\"inline-code\">LiquidTileRegistry.AllSpawnLayersMask</span> for every layer, or the same helper-mask pattern as tiles/buildings when you need specific layers.</td></tr>\n            <tr><td><span class=\"inline-code\">MaxFloodFill</span></td><td><span class=\"inline-code\">int</span></td><td>Default maximum fill size for <span class=\"inline-code\">FloodFill</span> and worldgen placement.</td></tr>\n          </tbody>\n        </table>\n      </div>\n    </section>\n\n    <section class=\"lesson-card\">\n      <h2>Touch callbacks</h2>\n      <p>The callback context reports the block position, world position, custom world byte, delta time, and whether the body just entered or exited. Use it for special logic that should happen on top of the built-in wetness, sickness, disinfect, or slip behavior.</p>\n      <pre><code>LiquidTileRegistry.Register(\"healingpool\", new CustomLiquidTileInfo\n{\n    LiquidId = \"disinfectant\",\n    WetnessPerSecond = 8f,\n    DisinfectPerSecond = 20f,\n    OnEnter = (body, context) =>\n    {\n        body.talker.Say(\"Warm...\");\n    },\n    OnTouch = (body, context) =>\n    {\n        if (body.health < 100f)\n            body.health += 2f * context.DeltaTime;\n    },\n    OnExit = (body, context) =>\n    {\n        body.happiness += 1f;\n    }\n});</code></pre>\n    </section>\n\n    <section class=\"lesson-card\">\n      <h2>Persistence and multiplayer</h2>\n      <p>Liquid tile definitions, their world-byte mapping, and current custom-liquid cells are already included in CUCoreLib's built-in save and multiplayer snapshot flow. The registry registers a world save provider during startup, and the multiplayer sync registry includes a dedicated <span class=\"inline-code\">liquidtiles</span> module.</p>\n      <p>The one important caveat is content reload: strict replayable content reload does not support liquid tile registration right now, so <span class=\"inline-code\">LiquidTileRegistry.Register()</span> intentionally rejects that path.</p>\n    </section>\n  ",
  "bodyText": "Liquids? Tiles? Why not combine the two? LiquidTileRegistry is CUCoreLib's world-liquid API. It registers custom fluid cells in the vanilla FluidManager , gives them a stable custom world byte, and patches the normal liquid systems so they can render, be drunk, fill containers, and affect bodies standing in them. This is different from Liquid API . A liquid registration defines what a logical liquid is. A liquid tile registration defines how that liquid behaves when it exists as a pool in the world. LiquidRegistry.Register(\"acidbrine\", new CustomLiquidInfo { name = \"Acid brine\", description = \"A shimmering pool of caustic industrial runoff.\", color = new Color(0.72f, 0.96f, 0.28f), valuePerLiter = 8f, onDrink = (ml, body) => { float dose = ml * 0.01f; body.Drink(dose * 2f); body.sicknessAmount += dose * 9f; body.happiness -= dose * 3f; } }); LiquidTileRegistry.Register(\"acidbrinepool\", new CustomLiquidTileInfo { LiquidId = \"acidbrine\", FillLiquidId = \"acidbrine\", Tint = new Color(0.8f, 1f, 0.45f, 1f), TemperaturePerSecond = 4f, SicknessPerSecond = 6f, WetnessPerSecond = 30f, SlipPerSecond = 0.35f, SpawnAmount = 0.15f }); What gets patched for you Once registered, CUCoreLib hooks the tile into the normal game liquid flow. FluidManager.WaterInfo reads your buoyancy and drag, LiquidColor and LiquidName use your registered display data, DrinkLiquid can consume the pool through the linked liquid, and Body.HandleVariableUpdates applies touch effects every frame. That means you do not need a separate Harmony patch just to make a custom swamp, acid pond, algae bath, or healing puddle act like a real world liquid. CustomLiquidTileInfo CustomLiquidTileInfo controls the world-facing behavior of the liquid tile. Most mods only need to set the linked liquid IDs, visuals, touch effects, and maybe worldgen. Field Type What it does LiquidId string Logical liquid used for display color fallback, drinking, and tooltip locale lookup. Defaults to the tile registration ID. FillLiquidId string Liquid ID loose containers receive when they fill from this tile. Defaults to LiquidId . Buoyancy float Returned through FluidManager.WaterInfo so bodies float or sink differently in this liquid. Drag float Movement resistance used while inside the liquid. PushBodies bool If true, CUCoreLib marks the body as inWater while touching the tile. WetnessPerSecond float Wetness added to the body each second. TemperaturePerSecond float Temperature change applied every second while touching the tile. SicknessPerSecond float Sickness buildup per second. DirtynessPerSecond float Dirtyness added per second. DisinfectPerSecond float Refreshes limb disinfect time while standing in the liquid. SlipPerSecond float Adds to body.liquidSlipTime for slippery liquids. RagdollBarDrainPerSecond float Subtracts from body.liquidRagdollBar over time. ConsumeOnDrink bool If true, drinking removes the world liquid cell after use. ConsumeOnFill bool Whether filling containers should consume the world liquid tile through the game's normal fill flow. OnDrinkOverride LiquidType.OnDrink Optional direct drink callback for the tile. If unset, the linked LiquidId onDrink is used instead. OnEnter Action<Body, LiquidTileTouchContext> Called once when a body steps into this liquid tile. OnTouch Action<Body, LiquidTileTouchContext> Called every frame while a body remains on the tile, after the built-in wetness/sickness/slip effects. OnExit Action<Body, LiquidTileTouchContext> Called once when a body leaves the tile. Visuals Custom liquid tiles keep using the game's fluid rendering path, but you can steer how the visuals are sourced. The final display color is the linked liquid's color multiplied by Tint . When you need a real custom surface instead of \"vanilla water, but with a different texture\", use LiquidVisualHelper to build a dedicated VisualMaterial from an embedded texture, loose file, or explicit shader name. Material acidMaterial = LiquidVisualHelper.CreateLiquidMaterialFromEmbeddedTexture( \"Images.acidbrine-surface.png\", shaderName: \"Legacy Shaders/Particles/Alpha Blended\" ); LiquidTileRegistry.Register(\"acidbrinepool\", new CustomLiquidTileInfo { LiquidId = \"acidbrine\", FillLiquidId = \"acidbrine\", VisualMode = LiquidTileVisualMode.Material, VisualMaterial = acidMaterial, Tint = Color.white }); Field Type What it does VisualMode LiquidTileVisualMode Chooses whether the tile reuses an existing vanilla fluid visual, a custom material, a sprite texture, or a generated high-res image. ExistingVisualLiquidByte byte Base vanilla liquid visual to clone from. Defaults to 1 which is water. Tint Color Multiplier applied to the linked liquid color for fluid rendering and tile tooltip color. VisualMaterial Material Material override used when VisualMode is Material . This is the full custom surface path. VisualSprite Sprite Sprite texture override used when VisualMode is Sprite . It still reuses the current fluid material. HighResImage Texture2D Texture override used when VisualMode is HighResImageGenerated . It still reuses the current fluid material. Most mods should start with ExistingLiquidPlusTint and move to VisualMaterial when the liquid needs a truly distinct surface look or a custom shader path. Placement and worldgen Use LiquidTileRegistry.Place(id, blockPos) to place one custom liquid cell into an empty world block, or FloodFill(id, start, maxFill) to spread the liquid outward using the vanilla fill simulation. For terrain generation, set SpawnAmount above 0f . CUCoreLib patches WorldGeneration.GenerateOres() and calls world.PlaceLiquids(...) for each registered tile that matches the current biome layer mask. Field Type What it does SpawnAmount float How often the liquid generates during world creation. Values above 0f opt into generation. SpawnLayers int Layer mask controlling which biome depths can spawn this liquid. Use LiquidTileRegistry.AllSpawnLayersMask for every layer, or the same helper-mask pattern as tiles/buildings when you need specific layers. MaxFloodFill int Default maximum fill size for FloodFill and worldgen placement. Touch callbacks The callback context reports the block position, world position, custom world byte, delta time, and whether the body just entered or exited. Use it for special logic that should happen on top of the built-in wetness, sickness, disinfect, or slip behavior. LiquidTileRegistry.Register(\"healingpool\", new CustomLiquidTileInfo { LiquidId = \"disinfectant\", WetnessPerSecond = 8f, DisinfectPerSecond = 20f, OnEnter = (body, context) => { body.talker.Say(\"Warm...\"); }, OnTouch = (body, context) => { if (body.health { body.happiness += 1f; } }); Persistence and multiplayer Liquid tile definitions, their world-byte mapping, and current custom-liquid cells are already included in CUCoreLib's built-in save and multiplayer snapshot flow. The registry registers a world save provider during startup, and the multiplayer sync registry includes a dedicated liquidtiles module. The one important caveat is content reload: strict replayable content reload does not support liquid tile registration right now, so LiquidTileRegistry.Register() intentionally rejects that path.",
  "codeTitle": "RegisterLiquidTiles.cs",
  "codeLanguage": "csharp",
  "code": "using CUCoreLib.Data;\nusing CUCoreLib.Helpers;\nusing CUCoreLib.Registries;\nusing UnityEngine;\n\nprivate void RegisterLiquidTiles()\n{\n    LiquidRegistry.Register(\"acidbrine\", new CustomLiquidInfo\n    {\n        name = \"Acid brine\",\n        description = \"A shimmering pool of caustic industrial runoff.\",\n        color = new Color(0.72f, 0.96f, 0.28f),\n        valuePerLiter = 8f,\n        onDrink = (ml, body) =>\n        {\n            float dose = ml * 0.01f;\n\n            body.Drink(dose * 2f);\n            body.sicknessAmount += dose * 9f;\n            body.happiness -= dose * 3f;\n        }\n    });\n\n    LiquidTileRegistry.Register(\"acidbrinepool\", new CustomLiquidTileInfo\n    {\n        LiquidId = \"acidbrine\",\n        FillLiquidId = \"acidbrine\",\n        Buoyancy = 0.55f,\n        Drag = 0.94f,\n        PushBodies = true,\n        WetnessPerSecond = 30f,\n        TemperaturePerSecond = 4f,\n        SicknessPerSecond = 6f,\n        SlipPerSecond = 0.35f,\n        VisualMode = LiquidTileVisualMode.Material,\n        VisualMaterial = LiquidVisualHelper.CreateLiquidMaterialFromEmbeddedTexture(\n            \"Images.acidbrine-surface.png\",\n            shaderName: \"Legacy Shaders/Particles/Alpha Blended\"\n        ),\n        Tint = Color.white,\n        SpawnAmount = 0.15f,\n        MaxFloodFill = 96,\n        OnEnter = (body, context) =>\n        {\n            body.talker.Say(\"Ow.\");\n        },\n        OnTouch = (body, context) =>\n        {\n            body.health -= 4f * context.DeltaTime;\n        }\n    });\n\n    LiquidTileRegistry.Place(\"acidbrinepool\", new Vector2Int(128, 42));\n    LiquidTileRegistry.FloodFill(\"acidbrinepool\", new Vector2Int(132, 42));\n}",
  "interactiveDefaults": null
}
