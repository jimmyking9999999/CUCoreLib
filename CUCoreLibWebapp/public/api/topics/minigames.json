{
  "id": "minigames",
  "label": "Minigames",
  "crumb": "Misc / API",
  "title": "Minigame API",
  "lead": "Shared helpers for creating custom minigames on top of the game's vanilla Minigame base.",
  "appUrl": "/docs/minigames/",
  "machineUrl": "/api/topics/minigames.json",
  "bodyHtml": "\n    <section class=\"lesson-card\">\n      <h2>What this wraps</h2>\n      <p>THIS PAGE IS IN DEVELOPMENT!</p>\n      <p>Custom minigames are planned to be implemented with a dedicated Unity tool, whilst the current api acts as a (pretty limited) wrapper around the vanilla system. </p>\n      <p>It is advised to either create your own minigame with raw code, or wait until the dedicated tool is released. Thanks! </p>\n      <p>Information below is retained for reference, but will soon be mostly outdated and unsupported.</p>\n\n      <p>Some important vanilla hooks for minigames (for your own implementation) are <span class=\"inline-code\">Start()</span>, <span class=\"inline-code\">Update()</span>, <span class=\"inline-code\">PhysicsUpdate()</span>, <span class=\"inline-code\">HandType()</span>, <span class=\"inline-code\">GuideLocaleString()</span>, <span class=\"inline-code\">NeedsItem()</span>, and <span class=\"inline-code\">CanExit()</span>.</p>\n    </section>\n\n    \n\n    \n    <section class=\"lesson-card\">\n      <h2>Bundled screen prefabs</h2>\n      <p>If your minigame UI lives in an AssetBundle instead of the game's normal <span class=\"inline-code\">Resources</span> path, register the bundle once during plugin setup and then use either the typed bundled helper or the reserved <span class=\"inline-code\">cclbundle://</span> screen ID.</p>\n      <pre><code>private void Awake()\n{\n    AssetLoader.RegisterBundleFromPluginFolder(this, \"glassworks.minigames\", \"Bundles/glassworks-minigames\");\n}</code></pre>\n      <pre><code>public override void Start(CUCoreMinigameSession session)\n{\n    session.TryCreateScreen(\"cclbundle://glassworks.minigames/WireSpliceScreen\");\n}</code></pre>\n      <p>Both paths instantiate the prefab under <span class=\"inline-code\">minigameScreen</span>, move it to sibling index <span class=\"inline-code\">0</span>, and update <span class=\"inline-code\">spawnedMiniGame</span> so the rest of CUCoreLib's lookup helpers behave exactly the same as a vanilla screen.</p>\n    </section>\n\n    <section class=\"lesson-card\">\n      <h2>Starting a minigame</h2>\n      <p>Use the runner guard so you do not accidentally stack minigames. If the runner is busy, the start request fails cleanly.</p>\n      <pre><code>if (CUCoreMinigames.TryStartDefinition(() => new WireSpliceMinigame()))\n{\n    // Started successfully.\n}</code></pre>\n    </section>\n  ",
  "bodyText": "What this wraps THIS PAGE IS IN DEVELOPMENT! Custom minigames are planned to be implemented with a dedicated Unity tool, whilst the current api acts as a (pretty limited) wrapper around the vanilla system. It is advised to either create your own minigame with raw code, or wait until the dedicated tool is released. Thanks! Information below is retained for reference, but will soon be mostly outdated and unsupported. Some important vanilla hooks for minigames (for your own implementation) are Start() , Update() , PhysicsUpdate() , HandType() , GuideLocaleString() , NeedsItem() , and CanExit() . Bundled screen prefabs If your minigame UI lives in an AssetBundle instead of the game's normal Resources path, register the bundle once during plugin setup and then use either the typed bundled helper or the reserved cclbundle:// screen ID. private void Awake() { AssetLoader.RegisterBundleFromPluginFolder(this, \"glassworks.minigames\", \"Bundles/glassworks-minigames\"); } public override void Start(CUCoreMinigameSession session) { session.TryCreateScreen(\"cclbundle://glassworks.minigames/WireSpliceScreen\"); } Both paths instantiate the prefab under minigameScreen , move it to sibling index 0 , and update spawnedMiniGame so the rest of CUCoreLib's lookup helpers behave exactly the same as a vanilla screen. Starting a minigame Use the runner guard so you do not accidentally stack minigames. If the runner is busy, the start request fails cleanly. if (CUCoreMinigames.TryStartDefinition(() => new WireSpliceMinigame())) { // Started successfully. }",
  "codeTitle": "CustomMinigame.cs",
  "codeLanguage": "csharp",
  "code": "using System.Collections.Generic;\nusing CUCoreLib.Helpers;\nusing UnityEngine;\nusing UnityEngine.EventSystems;\n\npublic sealed class WireSpliceMinigame : CUCoreMinigameDefinition\n{\n    public override Minigame.HandSpriteType HandType(CUCoreMinigameSession session)\n    {\n        return Minigame.HandSpriteType.Tweezers;\n    }\n\n    public override bool NeedsItem(CUCoreMinigameSession session)\n    {\n        return false;\n    }\n\n    public override string GuideLocaleKey(CUCoreMinigameSession session)\n    {\n        return \"wireSpliceGuide\";\n    }\n\n    public override void Start(CUCoreMinigameSession session)\n    {\n        session.TryCreateBundledScreen(\"glassworks.minigames\", \"WireSpliceScreen\");\n    }\n\n    public override void Update(CUCoreMinigameSession session, List<RaycastResult> uiCasts)\n    {\n        if (Input.GetKeyDown(KeyCode.Escape) && CanExit())\n        {\n            session.End();\n        }\n    }\n}\n\npublic static class WireSpliceStarter\n{\n    public static bool Start()\n    {\n        return CUCoreMinigames.TryStartDefinition(() => new WireSpliceMinigame());\n    }\n}",
  "interactiveDefaults": null
}
