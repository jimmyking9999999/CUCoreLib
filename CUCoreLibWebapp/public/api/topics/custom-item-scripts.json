{
  "id": "custom-item-scripts",
  "label": "Creating Custom Item Scripts",
  "crumb": "Items / Liquids",
  "title": "Creating Custom Item Scripts",
  "lead": "Attach MonoBehaviours to custom items, read custom item metadata, and structure wearable runtime logic.",
  "appUrl": "/docs/custom-item-scripts/",
  "machineUrl": "/api/topics/custom-item-scripts.json",
  "bodyHtml": "\n    <section class=\"lesson-card\">\n      <h2>When to use a custom item script</h2>\n      <p>Use a custom item script when plain <span class=\"inline-code\">useAction</span>, <span class=\"inline-code\">useLimbAction</span>, <span class=\"inline-code\">ToolProperties</span>, or wearable stats are not enough. A script is the right tool when the item needs its own runtime state, periodic logic, event-style reactions, or behavior that depends on whether it is lying in the world, held, or worn.</p>\n      <p>CUCoreLib adds item scripts through <span class=\"inline-code\">CustomItemInfo.AddSpawnComponent&lt;T&gt;()</span>. That helper stores the correct assembly-qualified type name in <span class=\"inline-code\">SpawnComponents</span>, and CUCoreLib attaches the <span class=\"inline-code\">MonoBehaviour</span> to each spawned item instance the first time the item appears.</p>\n      <p>If you have not read the broader custom-item surface yet, keep the <a href=\"/docs/advanced-item/\" data-page=\"advanced-item\">Advanced Item API</a> page open beside this one. That page covers the item fields and built-in modules; this page is specifically about the script side.</p>\n    </section>\n\n    <section class=\"lesson-card\">\n      <h2>How the script gets attached</h2>\n      <p>Register the item normally, then chain <span class=\"inline-code\">AddSpawnComponent&lt;YourScript&gt;()</span> on the <span class=\"inline-code\">CustomItemInfo</span> object. Your script runs on the spawned item GameObject, so you can use standard Unity callbacks like <span class=\"inline-code\">Awake</span>, <span class=\"inline-code\">Start</span>, and <span class=\"inline-code\">Update</span>.</p>\n      <pre><code>ItemRegistry.Register(\n    \"stormmantle\",\n    new CustomItemInfo\n    {\n        fullName = \"Storm Mantle\",\n        category = \"tool\",\n        wearable = true,\n        wearableCanBeHeld = true,\n        desiredWearLimb = \"UpTorso\",\n        wearSlotId = \"outertorso\"\n    }\n    .AddSpawnComponent&lt;StormMantleScript&gt;(),\n    icon\n);</code></pre>\n      <p>The script is added to every real item instance. That means dropped items, spawned loot, save-loaded items, and equipped wearables all use the same script class.</p>\n    </section>\n\n    <section class=\"lesson-card\">\n      <h2>Reading the item and its custom state</h2>\n      <p>Inside the script, cache the attached <span class=\"inline-code\">Item</span> component first. <span class=\"inline-code\">CustomData</span> now acts as inline defaults for each spawned item instance, so use <span class=\"inline-code\">ItemRegistry.GetCustomData&lt;T&gt;</span> or <span class=\"inline-code\">ItemRegistry.SetCustomData(...)</span> when you want friendly per-item reads and writes without hard-coding everything directly into the script.</p>\n      <pre><code>private Item item;\nprivate float warmthBonus;\n\nprivate void Awake()\n{\n    item = GetComponent&lt;Item&gt;();\n\n    if (item != null)\n        warmthBonus = ItemRegistry.GetCustomData(item, \"warmthBonus\", 0.25f);\n}</code></pre>\n      <p>Use <span class=\"inline-code\">CustomData</span> when you want inline defaults that each item instance can later read or mutate independently. This is item-only behavior; tile <span class=\"inline-code\">CustomData</span> still means registration metadata.</p>\n      <p>If you need to adjust a vanilla item stat from the script, remember that the field usually lives on <span class=\"inline-code\">item.Stats</span>, not directly on the <span class=\"inline-code\">Item</span> component. For example, use <span class=\"inline-code\">item.Stats.wearableIsolation = 0.20f;</span> rather than <span class=\"inline-code\">item.wearableIsolation = 0.20f;</span>.</p>\n    </section>\n\n    <section class=\"lesson-card\">\n      <h2>Equippable example</h2>\n      <p>This example registers a wearable mantle and adds a script that only applies its effect while the item is actually worn. The script detects the worn state by checking whether the item sits under a <span class=\"inline-code\">Limb</span> transform, which matches the same parentage CUCoreLib already uses for worn-sprite refresh.</p>\n      <pre><code>ItemRegistry.Register(\n    \"stormmantle\",\n    new CustomItemInfo\n    {\n        fullName = \"Storm Mantle\",\n        description = \"A lined mantle that steadies the body in cold weather.\",\n        category = \"tool\",\n        wearable = true,\n        wearableCanBeHeld = true,\n        desiredWearLimb = \"UpTorso\",\n        wearSlotId = \"outertorso\",\n        wearableIsolation = 0.16f,\n        wearableArmor = 0.08f,\n        weight = 1.3f,\n        value = 24,\n        tags = \"cangetwet\",\n        rec = new Recognition(5),\n        CustomData =\n        {\n            [\"warmthBonus\"] = 0.35f\n        }\n    }\n    .AddSpawnComponent&lt;StormMantleScript&gt;(),\n    AssetLoader.LoadEmbeddedSprite(\"Images.stormmantle.png\")\n);</code></pre>\n      <p>The paired script can then grant a mild warmth effect only while equipped:</p>\n      <pre><code>public sealed class StormMantleScript : MonoBehaviour\n{\n    private Item item;\n    private float warmthBonus = 0.25f;\n\n    private void Awake()\n    {\n        item = GetComponent&lt;Item&gt;();\n\n        if (item != null)\n            warmthBonus = ItemRegistry.GetCustomData(item, \"warmthBonus\", 0.25f);\n    }\n\n    private void Update()\n    {\n        Limb wornLimb = item != null && item.transform.parent != null\n            ? item.transform.parent.GetComponent&lt;Limb&gt;()\n            : null;\n        if (wornLimb == null) return;\n\n        Body body = wornLimb.body;\n        if (body == null) return;\n\n        body.clothingTemperature += warmthBonus * Time.deltaTime;\n    }\n}</code></pre>\n      <p>This pattern works well for passive wearable effects: warmth, movement modifiers, periodic healing, visual state, or compatibility hooks into other systems.</p>\n    </section>\n\n    <section class=\"lesson-card\">\n      <h2>Practical guidance</h2>\n      <ul>\n        <li>Cache <span class=\"inline-code\">Item</span> and any other required components in <span class=\"inline-code\">Awake</span> or <span class=\"inline-code\">Start</span> instead of calling <span class=\"inline-code\">GetComponent</span> every frame.</li>\n        <li>Keep wearable-only behavior behind a worn-state check. The same item script also exists while the item is dropped or sitting in inventory.</li>\n        <li>Use <span class=\"inline-code\">CustomData</span> for inline per-item defaults, then read or mutate them through <span class=\"inline-code\">ItemRegistry.GetCustomData</span> and <span class=\"inline-code\">ItemRegistry.SetCustomData</span>.</li>\n        <li>For vanilla item stats such as <span class=\"inline-code\">wearableIsolation</span> or <span class=\"inline-code\">wearableArmor</span>, mutate <span class=\"inline-code\">item.Stats</span>, not the <span class=\"inline-code\">Item</span> component itself.</li>\n        <li>If your script needs save data, use CUCoreLib's save/status systems. A <span class=\"inline-code\">MonoBehaviour</span> field alone is not a save format.</li>\n        <li>Reach for plain <span class=\"inline-code\">useAction</span> or built-in item modules first when they already solve the problem. Scripts are for the cases that actually need runtime behavior.</li>\n      </ul>\n    </section>\n  ",
  "bodyText": "When to use a custom item script Use a custom item script when plain useAction , useLimbAction , ToolProperties , or wearable stats are not enough. A script is the right tool when the item needs its own runtime state, periodic logic, event-style reactions, or behavior that depends on whether it is lying in the world, held, or worn. CUCoreLib adds item scripts through CustomItemInfo.AddSpawnComponent<T>() . That helper stores the correct assembly-qualified type name in SpawnComponents , and CUCoreLib attaches the MonoBehaviour to each spawned item instance the first time the item appears. If you have not read the broader custom-item surface yet, keep the Advanced Item API page open beside this one. That page covers the item fields and built-in modules; this page is specifically about the script side. How the script gets attached Register the item normally, then chain AddSpawnComponent<YourScript>() on the CustomItemInfo object. Your script runs on the spawned item GameObject, so you can use standard Unity callbacks like Awake , Start , and Update . ItemRegistry.Register( \"stormmantle\", new CustomItemInfo { fullName = \"Storm Mantle\", category = \"tool\", wearable = true, wearableCanBeHeld = true, desiredWearLimb = \"UpTorso\", wearSlotId = \"outertorso\" } .AddSpawnComponent<StormMantleScript>(), icon ); The script is added to every real item instance. That means dropped items, spawned loot, save-loaded items, and equipped wearables all use the same script class. Reading the item and its custom state Inside the script, cache the attached Item component first. CustomData now acts as inline defaults for each spawned item instance, so use ItemRegistry.GetCustomData<T> or ItemRegistry.SetCustomData(...) when you want friendly per-item reads and writes without hard-coding everything directly into the script. private Item item; private float warmthBonus; private void Awake() { item = GetComponent<Item>(); if (item != null) warmthBonus = ItemRegistry.GetCustomData(item, \"warmthBonus\", 0.25f); } Use CustomData when you want inline defaults that each item instance can later read or mutate independently. This is item-only behavior; tile CustomData still means registration metadata. If you need to adjust a vanilla item stat from the script, remember that the field usually lives on item.Stats , not directly on the Item component. For example, use item.Stats.wearableIsolation = 0.20f; rather than item.wearableIsolation = 0.20f; . Equippable example This example registers a wearable mantle and adds a script that only applies its effect while the item is actually worn. The script detects the worn state by checking whether the item sits under a Limb transform, which matches the same parentage CUCoreLib already uses for worn-sprite refresh. ItemRegistry.Register( \"stormmantle\", new CustomItemInfo { fullName = \"Storm Mantle\", description = \"A lined mantle that steadies the body in cold weather.\", category = \"tool\", wearable = true, wearableCanBeHeld = true, desiredWearLimb = \"UpTorso\", wearSlotId = \"outertorso\", wearableIsolation = 0.16f, wearableArmor = 0.08f, weight = 1.3f, value = 24, tags = \"cangetwet\", rec = new Recognition(5), CustomData = { [\"warmthBonus\"] = 0.35f } } .AddSpawnComponent<StormMantleScript>(), AssetLoader.LoadEmbeddedSprite(\"Images.stormmantle.png\") ); The paired script can then grant a mild warmth effect only while equipped: public sealed class StormMantleScript : MonoBehaviour { private Item item; private float warmthBonus = 0.25f; private void Awake() { item = GetComponent<Item>(); if (item != null) warmthBonus = ItemRegistry.GetCustomData(item, \"warmthBonus\", 0.25f); } private void Update() { Limb wornLimb = item != null && item.transform.parent != null ? item.transform.parent.GetComponent<Limb>() : null; if (wornLimb == null) return; Body body = wornLimb.body; if (body == null) return; body.clothingTemperature += warmthBonus * Time.deltaTime; } } This pattern works well for passive wearable effects: warmth, movement modifiers, periodic healing, visual state, or compatibility hooks into other systems. Practical guidance Cache Item and any other required components in Awake or Start instead of calling GetComponent every frame. Keep wearable-only behavior behind a worn-state check. The same item script also exists while the item is dropped or sitting in inventory. Use CustomData for inline per-item defaults, then read or mutate them through ItemRegistry.GetCustomData and ItemRegistry.SetCustomData . For vanilla item stats such as wearableIsolation or wearableArmor , mutate item.Stats , not the Item component itself. If your script needs save data, use CUCoreLib's save/status systems. A MonoBehaviour field alone is not a save format. Reach for plain useAction or built-in item modules first when they already solve the problem. Scripts are for the cases that actually need runtime behavior.",
  "codeTitle": "StormMantleScript.cs",
  "codeLanguage": "csharp",
  "code": "using BepInEx;\nusing CUCoreLib.Data;\nusing CUCoreLib.Helpers;\nusing CUCoreLib.Registries;\nusing UnityEngine;\n\n[BepInPlugin(\"com.example.stormmantle\", \"Storm Mantle Example\", \"1.0.0\")]\n[BepInDependency(\"net.cucorelib\", BepInDependency.DependencyFlags.HardDependency)]\npublic sealed class Plugin : BaseUnityPlugin\n{\n    private void Awake()\n    {\n        RegisterStormMantle();\n    }\n\n    private void RegisterStormMantle()\n    {\n        ItemRegistry.Register(\n            \"stormmantle\",\n            new CustomItemInfo\n            {\n                fullName = \"Storm Mantle\",\n                description = \"A lined mantle that steadies the body in cold weather.\",\n                category = \"tool\",\n                wearable = true,\n                wearableCanBeHeld = true,\n                desiredWearLimb = \"UpTorso\",\n                wearSlotId = \"outertorso\",\n                wearableIsolation = 0.16f,\n                wearableArmor = 0.08f,\n                weight = 1.3f,\n                value = 24,\n                tags = \"cangetwet\",\n                rec = new Recognition(5),\n                CustomData =\n                {\n                    [\"warmthBonus\"] = 0.35f\n                }\n            }\n            .AddSpawnComponent<StormMantleScript>(),\n            AssetLoader.LoadEmbeddedSprite(\"Images.stormmantle.png\")\n        );\n    }\n}\n\npublic sealed class StormMantleScript : MonoBehaviour\n{\n    private Item item;\n    private float warmthBonus = 0.25f;\n\n    private void Awake()\n    {\n        item = GetComponent<Item>();\n\n        if (item != null)\n            warmthBonus = ItemRegistry.GetCustomData(item, \"warmthBonus\", 0.25f);\n    }\n\n    private void Update()\n    {\n        Limb wornLimb = item != null && item.transform.parent != null\n            ? item.transform.parent.GetComponent<Limb>()\n            : null;\n        if (wornLimb == null) return;\n\n        Body body = wornLimb.body;\n        if (body == null) return;\n\n        body.clothingTemperature += warmthBonus * Time.deltaTime;\n    }\n}",
  "interactiveDefaults": null
}
