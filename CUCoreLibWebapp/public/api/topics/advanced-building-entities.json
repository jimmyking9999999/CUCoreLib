{
  "id": "advanced-building-entities",
  "label": "Advanced Buildings",
  "crumb": "World",
  "title": "Advanced BuildingEntity API",
  "lead": "Advanced building prefab hooks, worldgen details, loot crate patterns, and minigame-driven buildings.",
  "appUrl": "/docs/advanced-building-entities/",
  "machineUrl": "/api/topics/advanced-building-entities.json",
  "bodyHtml": "\n    <section class=\"lesson-card\">\n      <h2>When to use this page</h2>\n      <p>Use this page when your building needs prefab hooks, custom components, or special interaction logic. If the object is basically a prop or a vein, the normal BuildingEntity page is probably enough.</p>\n    </section>\n\n    <section class=\"lesson-card\">\n      <h2>Advanced building fields</h2>\n      <p>These are the knobs you reach for when a building needs extra Unity behavior, custom runtime wiring, or minigame-style interaction. Normal props usually do not need any of these.</p>\n      <div class=\"table-wrap\">\n        <table class=\"field-table\">\n          <thead>\n            <tr>\n              <th>Field</th>\n              <th>Type</th>\n              <th>What CUCoreLib uses it for</th>\n            </tr>\n          </thead>\n          <tbody>\n            <tr><td><span class=\"inline-code\">ConfigurePrefab</span></td><td><span class=\"inline-code\">Action&lt;GameObject&gt;</span></td><td>Runs once while the prefab is being built.</td></tr>\n            <tr><td><span class=\"inline-code\">ConfigureInstance</span></td><td><span class=\"inline-code\">Action&lt;GameObject&gt;</span></td><td>Runs on each spawned instance after instantiation.</td></tr>\n            <tr><td><span class=\"inline-code\">Components</span></td><td><span class=\"inline-code\">Type[]</span></td><td>Extra Unity components added to the prefab before it is cached.</td></tr>\n            <tr><td><span class=\"inline-code\">SpawnComponents</span></td><td><span class=\"inline-code\">List&lt;string&gt;</span></td><td>Qualified MonoBehaviour type names added to each spawned building instance the first time it wakes up.</td></tr>\n          </tbody>\n        </table>\n      </div>\n    </section>\n\n    <section class=\"lesson-card\">\n      <h2>Spawn-time MonoBehaviours</h2>\n      <p>Use <span class=\"inline-code\">SpawnComponents</span> when you want a quick per-instance script hook without manually wiring <span class=\"inline-code\">ConfigureInstance</span> or building a larger prefab setup path.</p>\n      <pre><code>BuildingEntityRegistry.Register(\"campheater\", new CustomBuildingEntityDefinition\n{\n    Name = \"Camp heater\",\n    Description = \"A small heater with a simple runtime script.\",\n    Sprite = mySprite,\n    Placement = BuildingPlacementType.Floor,\n    SpawnComponents = new List&lt;string&gt;\n    {\n        \"MyMod.CampHeaterRuntime, MyModAssembly\"\n    }\n});</code></pre>\n    </section>\n\n    <section class=\"lesson-card\">\n      <h2>Falling buildings</h2>\n      <p>Set <span class=\"inline-code\">AddRigidbody2D = true</span> for a falling building. CUCoreLib adds a dynamic <span class=\"inline-code\">Rigidbody2D</span> with mass <span class=\"inline-code\">10f</span>, gravity scale <span class=\"inline-code\">1f</span>, vanilla <span class=\"inline-code\">DamagingCrate</span> behavior, and the Ground layer used by vanilla crates, so it collides with terrain and characters. Set <span class=\"inline-code\">DamagePlayerOnImpact = true</span> to use vanilla stalactite limb damage on impacts faster than 6 units per second. Override <span class=\"inline-code\">RigidbodyBodyType</span>, <span class=\"inline-code\">RigidbodyGravityScale</span>, or <span class=\"inline-code\">Layer</span> only when the default falling setup is not what you need.</p>\n      <pre><code>BuildingEntityRegistry.Register(\"fallingrock\", new CustomBuildingEntityDefinition\n{\n    Sprite = rockSprite,\n    AddRigidbody2D = true,\n    DamagePlayerOnImpact = true\n});</code></pre>\n    </section>\n    \n    <section class=\"lesson-card\">\n      <h2>Keypad and lockpick minigames</h2>\n      <p>Vanilla buildings use an <span class=\"inline-code\">Openable</span> component for these interactions. If <span class=\"inline-code\">isKeypad</span> is true, <span class=\"inline-code\">Openable.OnUse()</span> launches <span class=\"inline-code\">KeypadMinigame</span>. Otherwise it launches <span class=\"inline-code\">LockpingMinigame</span> with <span class=\"inline-code\">lockpickAnglePrecision</span> scaled by the run setting <span class=\"inline-code\">lockpickprecision</span>.</p>\n      <p><span class=\"inline-code\">instantOpen</span> bypasses the minigame and just zeroes the building's health. That is the simplest pattern if you want a locked or coded building that still fits the normal game flow.</p>\n      <pre><code>public sealed class SafeDoorScript : MonoBehaviour\n{\n    private void Awake()\n    {\n        Openable openable = gameObject.AddComponent&lt;Openable&gt;();\n        openable.isKeypad = true;\n        openable.lockpickAnglePrecision = 1.25f;\n    }\n}</code></pre>\n      <p>For a keypad-style building, give the prefab an <span class=\"inline-code\">Openable</span> with <span class=\"inline-code\">isKeypad = true</span>. For a lockpick-style building, leave that false and adjust <span class=\"inline-code\">lockpickAnglePrecision</span> to tune how strict the minigame is.</p>\n    </section>\n  ",
  "bodyText": "When to use this page Use this page when your building needs prefab hooks, custom components, or special interaction logic. If the object is basically a prop or a vein, the normal BuildingEntity page is probably enough. Advanced building fields These are the knobs you reach for when a building needs extra Unity behavior, custom runtime wiring, or minigame-style interaction. Normal props usually do not need any of these. Field Type What CUCoreLib uses it for ConfigurePrefab Action<GameObject> Runs once while the prefab is being built. ConfigureInstance Action<GameObject> Runs on each spawned instance after instantiation. Components Type[] Extra Unity components added to the prefab before it is cached. SpawnComponents List<string> Qualified MonoBehaviour type names added to each spawned building instance the first time it wakes up. Spawn-time MonoBehaviours Use SpawnComponents when you want a quick per-instance script hook without manually wiring ConfigureInstance or building a larger prefab setup path. BuildingEntityRegistry.Register(\"campheater\", new CustomBuildingEntityDefinition { Name = \"Camp heater\", Description = \"A small heater with a simple runtime script.\", Sprite = mySprite, Placement = BuildingPlacementType.Floor, SpawnComponents = new List<string> { \"MyMod.CampHeaterRuntime, MyModAssembly\" } }); Falling buildings Set AddRigidbody2D = true for a falling building. CUCoreLib adds a dynamic Rigidbody2D with mass 10f , gravity scale 1f , vanilla DamagingCrate behavior, and the Ground layer used by vanilla crates, so it collides with terrain and characters. Set DamagePlayerOnImpact = true to use vanilla stalactite limb damage on impacts faster than 6 units per second. Override RigidbodyBodyType , RigidbodyGravityScale , or Layer only when the default falling setup is not what you need. BuildingEntityRegistry.Register(\"fallingrock\", new CustomBuildingEntityDefinition { Sprite = rockSprite, AddRigidbody2D = true, DamagePlayerOnImpact = true }); Keypad and lockpick minigames Vanilla buildings use an Openable component for these interactions. If isKeypad is true, Openable.OnUse() launches KeypadMinigame . Otherwise it launches LockpingMinigame with lockpickAnglePrecision scaled by the run setting lockpickprecision . instantOpen bypasses the minigame and just zeroes the building's health. That is the simplest pattern if you want a locked or coded building that still fits the normal game flow. public sealed class SafeDoorScript : MonoBehaviour { private void Awake() { Openable openable = gameObject.AddComponent<Openable>(); openable.isKeypad = true; openable.lockpickAnglePrecision = 1.25f; } } For a keypad-style building, give the prefab an Openable with isKeypad = true . For a lockpick-style building, leave that false and adjust lockpickAnglePrecision to tune how strict the minigame is.",
  "codeTitle": "RegisterAdvancedBuildings.cs",
  "codeLanguage": "csharp",
  "code": "using CUCoreLib.Data;\nusing CUCoreLib.Helpers;\nusing CUCoreLib.Registries;\nusing UnityEngine;\n\nprivate void RegisterAdvancedBuildings()\n{\n    BuildingEntityRegistry.Register(\"moddedlootcrate\", new CustomBuildingEntityDefinition\n    {\n        Name = \"Modded Loot Crate\",\n        Description = \"What'll it be this time?\",\n        Sprite = AssetLoader.LoadEmbeddedSprite(\"Images.modded-loot-crate.png\", 8f),\n        Health = 250f,\n        Metallic = true,\n        HitSoundReferenceId = \"metal\",\n        Placement = BuildingPlacementType.Floor,\n        GenerationStyle = BuildingGenerationStyle.DropPod, // Removes tiles around it when spawning\n        SpawnMinPerChunk = 0.01f,\n        SpawnMaxPerChunk = 0.015f,\n        SpawnLayers = BuildingEntityRegistry.AllLayersExcept(1, 2),\n        SurfaceOffset = 0.4f,\n        Components = new[] { typeof(ModdedLootCrateScript) }\n    });\n}\n\npublic sealed class ModdedLootCrateScript : MonoBehaviour\n{\n    private BuildingEntity building;\n    private UsableObject usable;\n    private readonly List<string> crateItems = new List<string>();\n\n    private void Awake()\n    {\n        building = GetComponent<BuildingEntity>();\n        usable = gameObject.AddComponent<UsableObject>();\n        usable.toggleString = \"Open crate\";\n        usable.didLangString = true;\n    }\n\n    private void Start()\n    {\n        crateItems.Clear();\n\n        foreach (string itemId in ItemRegistry.GetRegisteredItemIds())\n        {\n            if (CUCoreUtils.IsModdedItem(itemId))\n            {\n                crateItems.Add(itemId);\n            }\n        }\n            \n        string rewardId = crateItems[Random.Range(0, crateItems.Count)];\n        CustomInstantiate.InstantiateReturn(rewardId, transform.position + Vector3.up * 0.25f, transform.rotation, 1f);\n    }\n\n    public void OnUse()\n    {\n        if (MinigameBase.main.currentMinigame != null)\n        {\n            return;\n        }\n\n        building.description = \"A crate packed with only modded goods.\";\n        usable.didLangString = true;\n    }\n}",
  "interactiveDefaults": null
}
